import type { Metadata } from "next";
import { Inter, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Worksheet Maker — Professional School Worksheet Generator",
  description:
    "Paste multiple-choice questions and generate clean, perfectly formatted A4 PDF worksheets. Built for teachers.",
  keywords: [
    "worksheet generator",
    "MCQ",
    "school worksheet",
    "PDF worksheet",
    "teacher tools",
  ],
  authors: [{ name: "Omkar RG" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${inter.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
